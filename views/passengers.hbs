{{! 
The following areas were where sample code was obtained and edited for use in our project:
https://github.com/osu-cs340-ecampus/nodejs-starter-app
https://github.com/osu-cs340-ecampus/nodejs-starter-app/tree/main/Step%203%20-%20Integrating%20a%20Templating%20Engine%20(Handlebars)
}}

<div class="container">
    <div class="white-box">
        <h1>Passengers</h1>
        <p>This SELECTs from our Passengers Table</p>
        <table>
            <thead>

                <tr>
                    <th>Passenger ID</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Flight ID</th>
                </tr>
            </thead>
            <tbody>
                {{#each data}}
                <tr>
                    <td>{{this.passengerID}} </td>
                    <td>{{this.firstName}} </td>
                    <td>{{this.lastName}} </td>
                    <td>{{this.flightID}} </td>
                    <td><button class="delete-button" onclick="deletePassenger({{this.passengerID}})">Delete</button></td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        <script src="./js/delete_passenger.js"></script>
    </div>
</div>

<div class="container">
    <div class="white-box">
        <h2>Add a passenger</h2>
        <p>Please enter passenger information to add a passenger</p>
        <form id="add-passenger-form" method="POST" action="/add-passenger-form">
            <label for="input-firstName">First Name: </label>
            <input type="text" name="input-firstName" id="input-firstName">
            
            <label for="input-lastName">Last Name: </label>
            <input type="text" name="input-lastName" id="input-lastName">
            
            <br>
            <label for="flight-select">Select a Flight ID:</label>
            <select name="flight-select" id="flight-select">
                <option value=Null>No Flight booked Yet</option>
                {{#each flights}}
                    <option value={{this.flightID}}>{{this.flightID}}</option>
                {{/each}}
            </select>

            <br>
            <button class="button" type="submit">Add Passenger</button>
        </form>
    </div>
</div>

<div class="container">
    <div class="white-box">
        <h2>Updating a Passenger</h2>
        <p>To update a specific passenger, select them from the drop-down on the left and provide the updated information.</p>
        <form id="update-passenger-form-ajax">
            <label for="passenger-select">Select a passenger ID:</label>
            <select name="passenger-select" id="passenger-select">
                <option value="passenger-select-blank">Select a Passenger ID</option>
                {{#each data}}
                    <option value="{{this.passengerID}}">{{this.passengerID}}</option>
                {{/each}}
            </select>
            <br>

            <label for="input-firstName-update">First Name:</label>
            <input type="text" name="input-firstName-update" id="input-firstName-update">

            <label for="input-lastName-update">Last Name:</label>
            <input type="text" name="input-lastName-update" id="input-lastName-update">
            <br>

            <button class="button" type="submit">Update Passenger</button>
        </form>

        <script src="./js/update_passenger.js"></script>

    </div>
</div>

<a class="nav-button centered-button" href='/'>Return to Home</a>
